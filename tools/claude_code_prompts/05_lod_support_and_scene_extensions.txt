Task: turn the current placeholder LOD path into real, usable functionality.

Context:
- `DrawObject` already has `lods`, `numLods`, and `lodDist`, and the render path checks them.
- In practice, scene build code does not populate real LOD data, so the feature is mostly a shell.
- The project needs a path to reduce cost for heavier meshes at distance.
- Relevant files:
  - src/VulkanApp.cpp
  - src/VulkanApp.h
  - src/SceneLoader.cpp
  - src/SceneLoader.h
  - optionally maps/*.scene if backward-compatible syntax is added

Goal:
- Make LOD actually usable while keeping backward compatibility with existing scene files.

Requested changes:
1. Extend scene data structures so a mesh can optionally declare LOD variants and/or per-mesh LOD distances.
2. Parse the new syntax in `SceneLoader`.
3. Update scene building so `DrawObject.numLods`, `lods`, and `lodDist` are populated from real data when available.
4. Keep old scene files valid with no required changes.
5. If no LOD mesh is declared, current behavior should remain unchanged.
6. Add at least one example scene or example mesh declaration comment showing how to use the new LOD fields.

Constraints:
- Keep syntax simple.
- Do not require external asset tools in this part.
- Do not break current maps.

Definition of done:
- Release build succeeds.
- Existing maps still load.
- The renderer can now consume real LOD mesh declarations from scene files.
- LOD overlay/debug output is sufficient to confirm that the path is active when used.

Please include in your final response:
- files changed
- new scene syntax
- an example declaration

