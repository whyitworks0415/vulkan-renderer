Task: reduce per-frame CPU cost for medium-size maps without changing visual output.

Context:
- Current render path loops over `drawObjects` multiple times every frame.
- `std::sort` uses `glm::distance`, which performs a square root.
- Instancing groups are rebuilt from scratch each frame using `std::map<int, InstGroup>`.
- Several optimization flags exist, but the default runtime path still leaves a lot of CPU work on the table.
- Relevant files:
  - src/VulkanApp.cpp
  - src/VulkanApp.h

Goal:
- Lower CPU overhead for maps made of repeated objects.
- Preserve current rendering behavior unless the prompt explicitly calls for a safer default.

Requested changes:
1. Replace distance comparisons used only for ordering with squared-distance comparisons.
2. Remove avoidable per-frame heap churn in the render path:
   - reuse temporary vectors where practical
   - avoid rebuilding obvious derived state every frame if the scene is static
3. Precompute or cache instancing-group membership so the renderer does not rediscover group structure from scratch every frame.
4. Review whether frustum culling and instancing should be enabled by default for scene content that obviously benefits from them. If you change defaults, document the reasoning clearly.
5. Keep the code readable. Do not micro-optimize blindly.

Constraints:
- Do not add threading in this part.
- Do not change scene file syntax in this part.
- Keep occlusion culling behavior intact unless a minor safety fix is required.

Definition of done:
- Release build succeeds.
- Render path does less per-frame allocation and less redundant CPU work.
- Sorting no longer uses square roots where not needed.
- Instancing-group preparation is substantially cheaper than before.

Please include in your final response:
- files changed
- major CPU-path reductions you implemented
- whether you changed any default optimization flags

